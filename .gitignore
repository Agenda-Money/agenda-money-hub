# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-sandbox
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

.env
# Keep the sandbox build target tracked — it carries no secret, only the API
# host, and a server needs it to build the sandbox bundle.
!.env.sandbox
docs

# AI Assistant artifacts
.claude/
FRONTEND_AUDIT.md
tsc_output.txt
tsc_output_*.txt